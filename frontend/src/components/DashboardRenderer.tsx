/**
 * Dashboard renderer - now integrated into ChatMessage.
 * This component is kept for backward compatibility but returns null.
 */

export function DashboardRenderer() {
    // Dashboard rendering is now handled by ChatMessage component
    return null;
}
